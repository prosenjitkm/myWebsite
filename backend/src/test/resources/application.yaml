# Test-only overrides – never shipped to production.
spring:
  profiles:
    active: test
  datasource:
    url: jdbc:postgresql://localhost:5432/mywebsite
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret

app:
  jwt:
    secret: test-secret-key-at-least-32-characters-long!!
    expiration-ms: 86400000
